export const metadata = { title: 'Ponto — Grupo 364' };

// Quiosque roda fora do AppShell: sem sidebar, sem sessão de usuário.
export default function QuiosqueLayout({ children }) {
  return children;
}
